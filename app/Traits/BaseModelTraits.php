<?php

namespace App\Traits;

/**
 * This trait is used to add common methods to all models in the application,
 * allowing for a more DRY approach to coding. The use of traits is selected
 * so that even models that does not directly extend the Eloquent Model can
 * still use the methods defined in this trait.
 *
 * @package App\Traits
 */
trait BaseModelTraits {}
